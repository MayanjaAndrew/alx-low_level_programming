#include "main.h"
#include <string.h>

/**
 * _strlen - returns th elength of a string
 * @s: string to evaluate
 * Return: the lenght of the string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(*s);
	return (len);
}
